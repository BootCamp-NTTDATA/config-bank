server:
  port: 8080

eureka:
 client:
    serviceUrl:
      defaultZone: http://198.168.1.3:8761/eureka/
 instance:
    id: ${spring.application.name}:${spring.application.instance_id}:${random.value}
