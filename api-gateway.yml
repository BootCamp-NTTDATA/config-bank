server:
  port: 8080

eureka:
 client:
    serviceUrl:
      defaultZone: http://192.168.1.93:8761/eureka/
 instance:
    id: ${spring.application.name}:${spring.application.instance_id}:${random.value}
