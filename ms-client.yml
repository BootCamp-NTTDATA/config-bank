server:
  port: 8083

spring:
  data:
    mongodb:
      uri: mongodb+srv://admin:123456.@ms-client.xvrht.mongodb.net/myFirstDatabase?retryWrites=true&w=majority

eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://${eureka.instance.hostname}:${server.port}/eureka/
